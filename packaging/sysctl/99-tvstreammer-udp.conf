# TVStreammerSAT5 high-bitrate UDP/multicast ingest.
# UdpInput requests a 16 MiB SO_RCVBUF per live UDP socket. Linux clamps that
# request to net.core.rmem_max, so keep the maximum comfortably above it.
net.core.rmem_default=4194304
net.core.rmem_max=33554432
net.core.netdev_max_backlog=10000
